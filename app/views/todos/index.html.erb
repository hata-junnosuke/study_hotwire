<!-- 見出し -->
<h4 class="fw-bold">
  <span class="me-1">🐱</span>
  <span>ねこ</span>
</h4>

<!-- 一覧 -->
<div class="card shadow mt-3">
    <div class="card-header">
      <%= icon_with_text("table", "未完了") %>
    </div>

    <div class="card-body mx-3">
      <div class="row py-2">
        <div class="col-2 mt-auto">
          <%= "complete" %>
        </div>
        <div class="col-3 mt-auto">
          <%= "title" %>
        </div>
        <div class="col-3 mt-auto">
          <%= "content" %>
        </div>
        <div class="col-4 d-flex justify-content-end">
          <%= link_to icon_with_text("plus-circle", "登録"),
                      new_todo_path,
                      class: "btn btn-outline-primary"
          %>
        </div>
      </div>

      <div id="incomplete_todos">
        <%= render @todos.reject(&:complete) %>
      </div>
    </div>
</div>

<div class="card shadow mt-3" id="complete">
  <div class="card-header">
    <%= icon_with_text("table", "完了") %>
  </div>

  <div class="card-body mx-3">
    <div class="row py-2">
      <div class="col-2 mt-auto">
        <%= "complete" %>
      </div>
      <div class="col-3 mt-auto">
        <%= "title" %>
      </div>
      <div class="col-3 mt-auto">
        <%= "content" %>
      </div>
    </div>

    <div id="complete_todos">
      <%= render @todos.select(&:complete) %>
    </div>
  </div>
</div>
